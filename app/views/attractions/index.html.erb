<% Attraction.all.each do |attraction| %>
  <%= attraction.inspect %>
  <%= link_to "Go on #{attraction.name}", attraction unless @user.admin %>
  <%= link_to "Show #{attraction.name}", attraction %>
<% end %>

<%= link_to "New Attraction", new_attraction_path %>

<%= @user.inspect %>